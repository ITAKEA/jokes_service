responses:
  200:
    description: A random joke
    schema:
      type: object
      properties:
        id:
          type: integer
        setup:
          type: string
        punchline:
          type: string
